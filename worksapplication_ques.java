public class worksapplication_ques {
    //output the repeat number of two lists
    //
    //root1 root2
    int curr_num=Math.max(root.val,root2.val);
    if(roo1=root2){
        root1=root1.next;
        root2=root2.next;
    }



}
